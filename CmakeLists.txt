cmake_minimum_required(VERSION 3.29.0)

project(planning
VERSION 0.0
DESCRIPTION "a demo of planning"
HOMEPAGE_URL "https://github.com/sensenking/Cmake_planning_test"
LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 17)

#变量和目录设置
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${PROJECT_SOURCE_DIR}/bin)
set(Process_Dir ${CMAKE_SOURCE_DIR}/src/process)
set(Pm_Map_Dir ${CMAKE_SOURCE_DIR}/src/pm_map)


#第三方库

# 三方库
set(EIGEN3_INCLUDE_DIR "D:/eigen-3.4.0/eigen-3.4.0")
list(APPEND CMAKE_MODULE_PATH "${EIGEN3_INCLUDE_DIR}/cmake")
message("CMAKE_MODULE_PATH = ${CMAKE_MODULE_PATH}")

find_package(Eigen3 3.4 REQUIRED)

if(TARGET Eigen3::Eigen)
    message(STATUS "********** Eigen3 3.4 found in ${EIGEN3_INCLUDE_DIR} **********")
endif()

#添加子目录
add_subdirectory(src)

