project(process)

add_library(${PROJECT_NAME}
SHARED
process.cpp
)

target_link_directories(${PROJECT_NAME}
PUBLIC
${CMAKE_SOURCE_DIR}/src/pm_map
)
target_link_libraries(${PROJECT_NAME}
PUBLIC
pm_map
)